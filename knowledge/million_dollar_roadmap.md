# Стратегический Бизнес-План и Дорожная Карта успеха на $1 000 000 (The Million Dollar Roadmap)

*Этот документ непрерывно ведется, обновляется и актуализируется автономным **Агентом-Управляющим сайтом (Site Manager Agent)** как стратегический бизнес-журнал проекта `dalaman.info`.*

---

## 🌟 Северная Звезда (North Star Vision)
**Цель:** Превратить туристический портал **Dalaman Guide (`dalaman.info`)** в главный, самый полезный, единообразный и высокоприбыльный хаб региона **«От Датчи до Каша»** с долгосрочным потенциалом монетизации и капитализации в **$1 000 000**.
- **Принцип реализма:** Без пустых фантазий. Опора на реальный поток прилетающих в аэропорт Даламан (`DLM`), долгосрочное доверие аудитории, пользу и сильные партнерства.
- **Принцип системности:** Каждое внедрение делается единообразно для всего сайта. Запрещено бросать реформы на полпути.

---

## 🗺️ Генеральная Дорожная Карта по Стадиям

### 🚩 СТАДИЯ 1: Идеальное ядро, вложенность и 100% Google-Friendly SEO (Текущий статус: В РАБОТЕ / ДЕНЬ №1 АУДИТ ЗАВЕРШЕН)
*Цель стадии: подготовить проект к безупречному деплою на домен `dalaman.info`, чтобы Google сразу дал органический трафик, а пользователи получили идеальный UX.*
*Полная декомпозиция задач с оценками в токенах и часах находится в файле [`knowledge/master_backlog.md`](file:///c:/Users/Dimok/IdeaProjects/dalaman/knowledge/master_backlog.md).*
- [ ] **Батч 1.1:** Очистка 1-го экрана от бытовой инфраструктуры (Аптеки из `Header.tsx`, Больницы/Полиция из `LeftSidebar.tsx`, Заявки из `FloatingMenu.tsx`) и перенос в единый раздел «Справка и Инфраструктура» на 2–3 уровни. (`~18k токенов / ~35 мин`).
- [ ] **Батч 1.2:** Устранение бага выпадающего меню (`Hover-Gap Bug`) через сплошной `hover-bridge` в `Header.tsx` и `Navigation.tsx` + задержка закрытия (`debounce`). (`~22k токенов / ~45 мин`).
- [ ] **Батч 1.3:** Архитектурная чистка кодовой базы от мертвых артефактов (`src/[locale]/` и `src/components/`). (`~12k токенов / ~20 мин`).
- [ ] **Батч 1.4:** SEO-нормализация и добавление YAML frontmatter в первые 40 статей (включая восстановление 26 «слепых» статей без метаданных). (`~45k токенов / ~1.5 ч`).
- [ ] **Батч 1.5:** SEO-нормализация оставшихся 43 статей + их переводов (`.en.md`, `.de.md`, `.tr.md`). (`~45k токенов / ~1.5 ч`).
- [ ] **Батч 1.6:** Защита от инфляции (проверка лир на диапазоны), маркировка «Сгенерировано ИИ / AI Placeholder» на все временные фото и аудит 59 локаций в `content/locations`. (`~35k токенов / ~1.2 ч`).
- [ ] **Батч 1.7:** Пре-деплой валидация (`npm run build`), sitemap.xml, robots.txt и релиз в продакшн на домен `dalaman.info`. (`~25k токенов / ~50 мин`).

### 📈 СТАДИЯ 2: Первичная монетизация и трафик (Ожидание завершения Стадии 1)
*Цель стадии: конвертировать органический трафик в прибыль и пользу для туристов.*
- [ ] **Батч 2.1:** Партнерские программы (Travel Affiliates: Aviasales, Localrent, бронирование жилья). (`~40k токенов / ~1.5 ч`).
- [ ] **Батч 2.2:** Прямые комиссии и местная реклама («откаты» от служб такси из аэропорта Даламан, аренда яхт, экскурсии). (`~45k токенов / ~1.7 ч`).
- [ ] **Батч 2.3:** Интеграция расписания автобусов провинции Мугла (`Muğla App` / Muttaş/Havaş) и сервис поиска попутчиков. (`~50k токенов / ~2 ч`).

### 💎 СТАДИЯ 3: ИИ-инновации и масштабирование до $1 000 000 (Future Frontier)
*Цель стадии: закрепление лидерства и выход на глобальную капитализацию.*
- [ ] **Батч 3.1:** Внедрение передовых ИИ-решений (LLM-гид планировщик и открытый MCP-сервер `api/mcp/v1/dalaman` для внешних ИИ-агентов). (`~60k токенов / ~2.5 ч`).
- [ ] **Батч 3.2:** Превращение в PWA/Мобильное приложение с офлайн-картами и гидом. (`~40k токенов / ~1.5 ч`).

---

## 📅 Журнал Итераций и Текущий Статус (Iteration Log)

### 🟢 Текущий фокус Агента-Управляющего
* **Статус:** День №1 завершен. Проведен тотальный аудит, сформирован Мастер-Бэклог, Утреннее Дейли успешно проведено и утверждено.
* **Ближайший шаг (План на День №2 — Полная Суточная Загрузка):** Выполнить Батчи 1.1 (очистка 1-го экрана + Nöbetçi Eczane), 1.2 (фикс меню Hover-Gap) и 1.3 (чистка папок) — `~52к токенов / коммит №1`. После сброса квоты взять в работу Батч 1.4 (восстановление 26 «слепых» статей + SEO первых 40 статей) — `~45к токенов / коммит №2`. Итого за сутки вырабатывается 2 цикла квоты (`~97к токенов`) и делается 2 чистых коммита.
